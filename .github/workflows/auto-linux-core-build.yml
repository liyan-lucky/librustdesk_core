name: Auto trigger Linux core build

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths-ignore:
      - '.github/workflows/cleanup-releases.yml'
      - '.github/workflows/auto-linux-core-build.yml'
      - '.github/workflows/update-core-release-notes.yml'

permissions:
  actions: write
  contents: read

jobs:
  trigger:
    name: Trigger Linux core build
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Linux build workflow
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          ref_name="${{ github.ref_name }}"
          if [ -z "$ref_name" ]; then
            ref_name="main"
          fi
          gh workflow run build-core-linux.yml --repo "${{ github.repository }}" --ref "$ref_name"
